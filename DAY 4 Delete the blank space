#include <stdio.h>
#include <string.h>

int main() {
    char a[100];
    scanf("%[^\n]", a);
    int len = strlen(a);
    for (int i = 0; i < len; i++) {
        if (a[i] != ' ') {
            printf("%c", a[i]);
        }
    }
    return 0;
}
