#include <math.h>
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <assert.h>
#include <limits.h>
#include <stdbool.h>

int main() {
   char n[100];
    scanf("%s",n);
    int len=strlen(n);
    for(int i=len-1;i>=0;i--)
    {
        printf("%c",n[i]);
    }
    return 0;
}
